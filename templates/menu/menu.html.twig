
<nav class="navbar navbar-expand-sm navbar-dark bg-black fixed-top">
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <div class="container">
        <a href="{{ path('welcome') }}" class="navbar-brand">
            <img src="{{ asset('image/logo.png') }}" alt="Car Rental Logo" class="logo-img">
        </a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#mobile-nav">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="mobile-nav">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a href="{{ path('listeVoiture') }}" class="nav-link btn-black">Our Vehicles</a>
                </li>
                <li class="nav-item ">
    <a class="nav-link btn-black" href="{{ path('listeMarque') }}">Our Brands</a>
</li>

                <li class="nav-item">
                    <a href="{{ path('listeTarif') }}" class="nav-link btn-black">Our Prices</a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('listeLocation') }}" class="nav-link btn-black">Rentals</a>
                </li>

                {% if not app.user %}
                    <li class="nav-item">
                        <a href="{{ path('security_login') }}" class="btn btn-secondary btn-lg active">Connexion</a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('user_registration') }}" class="btn btn-secondary btn-lg active">Inscription</a>
                    </li>
                {% else %}
                    <li class="nav-item dropdown">
                        <a href="{{ path('security_login') }}" class="nav-link dropdown-toggle" id="userDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            {{ app.user.username }}
                        </a>
                        <div class="dropdown-menu" aria-labelledby="userDropdown">
                            <a href="{{ path('security_logout') }}" class="dropdown-item deconnex-link">Déconnexion</a>
                        </div>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>

<style>
    .logo-img {
        max-height: 60px; /* Adjust the maximum height as needed */
        margin-right: 30px; /* Add margin to the right of the logo */
    }

    .bg-black {
        background-color: #000; /* Black color code for the navbar */
    }

    .navbar {
        z-index: 1000; /* Adjust this value based on your layout */
    }

    .navbar-brand {
        font-size: 1.5rem; /* Adjust the font size as needed */
        font-weight: bold; /* Make the brand text bold */
    }

    .nav-link, .btn-black {
        font-size: 1.1rem; /* Adjust the font size as needed */
        margin-right: 15px; /* Add margin between nav items and buttons */
        color: #E9B824 !important; /* Yellow color code for text */
        font-weight: bold; /* Make the text bold */
    }

    /* Styling for Connexion link */
    .connex-link {
        color: #fff; /* White text for Connexion link */
        font-weight: bold; /* Make Connexion link bold */
    }

    /* Styling for Déconnexion link */
    .deconnex-link {
        color: #ff0000; /* Red text for Déconnexion link */
        font-weight: bold; /* Make Déconnexion link bold */
    }

    .dropdown-menu {
        background-color: #000; /* Black color for the dropdown menu */
    }

    .dropdown-item {
        color: #fff !important; /* White text for dropdown items */
        font-weight: bold; /* Make the text bold */
    }
</style>
